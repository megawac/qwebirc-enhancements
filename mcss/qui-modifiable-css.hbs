{{!--  
    * Note this is a handlebars template. Changing values in here requires a grunt rebuild
    * note the context (for property names) is the uiOptions values see ui/standardUI for implementation
    * update the Browser object is also now part of the context so you can do #if ie6 or something

    * Available helpers
    * see js/src/utils/Templates (github.com/megawac/qwebirc-enhancements/blob/master/js/src/util/Templates.js) 
    * colours will assume the base background by default
        * "$result": property, default -> str           get a property or its default val
        * "$hex": str ([colour]) -> hex                 get hex val
        * "$mix": str (colour), [type|colour] -> hex    mix colour and base colour by default
        * "$saturate": num, [type|colour] -> hex        saturate colour by delta (less than 1)
        * "$lighten": num, [type|colour] -> hex          lighten colour by delta (less than 1). Negative delta to darken
        * "$invert": [type|colour] -> hex               invert a colour or default
--}}

.qui .content {
    color: {{$hex 'font'}};
    background: {{$hex 'background'}};
}

.qui .lines {
    font-size: {{$result 'font_size' 12}}px;
}

.qui .lines .timestamp {
    display: {{#if show_timestamps}}'inline'{{else}}'none'{{/if}};
}

.qui .lines .highlight1 {
    background: {{$hue '-0.25'}};
}

.qui .lines .highlight2 {
    background: {{$hue '0.25'}};
}

.qui .lines .mentioned {
    background: {{$hue '0.5'}} !important;
}

.qui .properties {
    background: {{$saturate '0.04'}};
    border-top: thin solid {{$lighten '-0.1'}};
}

.qui .topic {
    color: {{$mix '4f4f4f' 'font'}};
    background: {{$hex 'background'}};
    border-bottom: thin dashed {{$lighten '-0.15'}};
}

{{! tab stuff }}
.qui .outertabbar, .qui .detached-window .header {
    color: {{$mix '0d0b0b' 'font'}};
    background: {{$saturate '0.1'}};
    border-bottom: thin solid {{$lighten '-0.05'}};
}

.qui .tabbar .tab {
    background: {{$lighten '-0.005'}};
    background: rgba(189, 189, 189, 0.1);
    border: thin solid {{$lighten '-0.1'}};
}

.qui .tabbar .selected.tab {
    color: {{$mix '333333' 'font'}};
}
{{!  tab stuff  }}

{{! nicklist }}

.qui .nicklist {
    border-left: thin solid {{$lighten '-0.15'}};
}

{{!--
.qui .nicklist .user, .qui .nick-menu {
    border: thin solid {{$lighten '-0.1'}};
}

.qui .nicklist .selected {
    color: {{$hex 'font'}};
    background: #fff;

    border-top: thin solid {{$lighten '-0.15'}};
    border-bottom: thin solid {{$lighten '-0.15'}};
}
--}}

.qui .dropdownmenu, .qui hr.lastpos {
    border: thin solid {{$lighten '-0.15'}};
    background: {{$hex 'background'}};
}

.qui .dropdownmenu a:hover {
    background: {{$saturate '40%'}};
}

{{! irc input stuff }}
{{! scary gradients }}

.qui .input {
    background: {{$saturate '0.04'}};
}

.qui .input div {
    border-top: thin solid {{$lighten '-0.15'}};
}

.qui hr.lastpos {
    border: thin solid {{$lighten '-0.15'}};
}

.qwebirc .input .decorated {
    background: {{$saturate '-0.15'}};
    {{! will be split using .split(',')}}
    {{#vendor-prefix '-webkit-,-moz-,-o-,-ms-,'}}
        background-image: {prefix}linear-gradient(bottom, {{$saturate '-0.10'}} 66%, {{$saturate '-0.15'}} 54%);
    }
    }
    {{/vendor-prefix}}
}

.qui .user {
    background: {{$saturate '-0.15'}};
}
<div class="container-fluid center">
    <form class="login">
        <h2>{{lang 'connectWelcome'}}</h2>
        <div class="control-group right">
            <label class="control-label" for="nickname">{{lang 'nickname'}}:
                <input type="text" class="form-control" data-validate="nick" name="basic" id="nickname" value="{{nickname}}" placeholder="{{lang 'nickname'}}" required />
            </label>
        </div>
        <div class="control-group right {{#unless full}}hidden{{/unless}}">
            <label class="control-label" for="username">{{lang 'username'}}:
                <input type="text" class="form-control" data-validate="username" name="full" id="username" value="{{username}}" placeholder="{{networkName}} {{lang 'username'}}">
            </label>
        </div>
        <div class="control-group right {{#unless full}}hidden{{/unless}}">
            <label class="control-label" for="password">{{lang 'password'}}:
                <input type="password" class="form-control" data-validate="password" name="full" id="password" value="{{password}}">
            </label>
        </div>
        <div class="authenticate">
            <label for="authenticate">{{lang 'enableAuth'}}<input type="checkbox" id="authenticate" {{check full}}></label>
        </div>
        <div>
            <input type="submit" value="Connect" class="btn btn-primary btn-smaller" />
        </div>
    </form>
    <div class="init-channels"><span>{{channels}}</span></div>
</div>  